# app.bundle.js is generated by esbuild (always LF). Pin its line endings so a
# Windows checkout can't rewrite it to CRLF and cause a false "out of sync" diff
# against the Linux CI build.
app.bundle.js text eol=lf
app.js text eol=lf
