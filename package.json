{
  "name": "veteran-career-path",
  "version": "1.0.0",
  "private": true,
  "description": "VeteranCareerPath.com static site + in-app React tools",
  "scripts": {
    "build": "esbuild app.js --loader:.js=jsx --format=iife --outfile=app.bundle.js",
    "build:check": "npm run build && git diff --exit-code -- app.bundle.js"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
